<section class="deck">
  <div class="name">
  <div class="card container-fluid">
    <div class="card-header">
      <a text="{{deck_name}}" target="_blank">{{deck_name}}</a>
    </div>
    <div class="card-body mx-auto overflow-hidden">
      <a href="/deck/{{id}}" class="btn bg-primary opn-deck">Open Deck</a>
      <p class="card-text"> {{card-info}} </p>

    </div>
  </div>
  {{!-- <div class="meta">
    <a href="/deck/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
    
  </div> --}}
  </div>
</section>